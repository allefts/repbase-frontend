import React from "react";
import styled from "styled-components";

const Events = () => {
  return <StyledEvents>Events</StyledEvents>;
};

const StyledEvents = styled.div``;

export default Events;
